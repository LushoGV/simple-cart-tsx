export interface Producto{
    id: string,
    title: string,
    price: number,
    description: string,
    image: string
  }

  export interface Props{
    id: string,
    title: string,
    price: number,
    description: string,
    image: string,
  }

